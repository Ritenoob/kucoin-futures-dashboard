source doc
code provided (server.js, index.html) and the existing PDF content, here are the four technical sections that need to be designed and added to the PDF.These sections document the ROI-based logic (introduced in v3.4.1), the Safety Architecture, and the Net P&L formulas required for precise break-even analysis.Section 8: ROI-Based Stop-Loss & Take-Profit Logic (v3.4.1)Context: Version 3.4.1 of the bot shifted from raw price-percentage stops to ROI-based stops. This ensures that a "2% Take Profit" means a 2% gain on the Account Equity, regardless of the leverage used.The Problem:In previous versions, setting a 1% Stop Loss on a 10x leveraged position resulted in a 10% loss of margin. As leverage increased, the risk on equity grew exponentially if the stop distance remained fixed in price terms.The Solution (Inverse Leverage Scaling):The bot now calculates the required price movement ($\Delta P$) by dividing the desired Return on Investment (ROI) by the leverage factor.Formulas:Let $R_{target}$ be the target ROI percentage (e.g., 2.0% for TP).Let $L$ be the Leverage (e.g., 10x).The required percentage move in price ($P_{\%}$) is:$$P_{\%} = \frac{R_{target}}{L}$$Therefore, the Price Targets are calculated as:For Long Positions:$$SL_{price} = Entry \times (1 - \frac{R_{risk}}{L})$$$$TP_{price} = Entry \times (1 + \frac{R_{reward}}{L})$$For Short Positions:$$SL_{price} = Entry \times (1 + \frac{R_{risk}}{L})$$$$TP_{price} = Entry \times (1 - \frac{R_{reward}}{L})$$Code Implementation:JavaScript// From server.js lines 925-929
const slPercent = (CONFIG.TRADING.INITIAL_SL_PERCENT / leverage) / 100;
const tpPercent = (CONFIG.TRADING.INITIAL_TP_PERCENT / leverage) / 100;
Section 9: Fee-Adjusted Break-Even MathContext: The current bot triggers break-even at unrealizedPnlPercent > 0.001. However, this is a "Gross Break-Even." To be truly risk-free, the bot must account for Maker/Taker fees on the Notional Value.The Fee Impact:Crypto futures fees are charged on the total position size ($Margin \times Leverage$).If the Taker Fee is $F_{taker}$ (e.g., 0.06%), the cost relative to your margin is amplified by leverage.The Formula:To cover entry and exit fees (assuming Taker for both to be safe), the Leveraged P&L % ($P n L_{lev}$) must exceed:$$BreakEven_{ROI} = (F_{entry} + F_{exit}) \times Leverage$$Example:Leverage: 10xFee: 0.06% (0.0006) per trade.Math: $(0.06\% + 0.06\%) \times 10 = 1.2\%$Design Implication:The bot's current trigger of 0.001% moves the stop to entry effectively, but if the stop is hit, the user loses the fees.Recommended Update for v3.5:Adjust BREAK_EVEN_TRIGGER to dynamically calculate:$$Trigger = (0.0006 \times 2 \times Leverage \times 100) + Buffer$$Section 10: Trailing Stop "Staircase" AlgorithmContext: The bot uses a discrete stepping function rather than a continuous slide. This reduces API spam and ensures meaningful profit locking.The Algorithm:The trailing logic checks if the current Leveraged ROI has crossed a new threshold defined by TRAILING_STEP_PERCENT (0.15%).Step Calculation:$$Steps = \lfloor \frac{CurrentROI\% - LastTrailLevel\%}{StepSize\%} \rfloor$$Stop Adjustment:If $Steps > 0$, the Stop Loss moves by:$$\Delta SL_{\%} = Steps \times MoveSize\%$$Visual Logic:Trigger: Position hits Break-Even.Wait: Price increases, ROI increases.Step: ROI crosses LastLevel + 0.15%.Action: Move SL up by 0.05% (Price distance).Reset: Set LastLevel = CurrentROI.This creates a "staircase" effect where the stop loss chases the price but maintains a widening buffer to allow for volatility as profits grow.Section 11: Auto-Leverage via Volatility (ATR)Context: The index.html file contains logic for an "Auto" leverage mode based on Average True Range (ATR).Logic:Leverage should be inversely proportional to market volatility. High volatility implies a higher chance of liquidation; therefore, leverage must be lowered.The Formula:The frontend calculates volatility as a percentage of price:$$Volatility_{\%} = (\frac{ATR_{14}}{Price}) \times 100$$The Tiered System:The bot applies a step-down function for leverage safety:Volatility (ATR %)Safe Leverage Cap< 0.5%50x0.5% - 1.0%25x1.0% - 2.0%15x2.0% - 3.0%10x> 5.0%3xRisk Multiplier:A user-defined scalar ($M_{risk}$) adjusts this baseline:$$FinalLeverage = \text{Clamp}(1, 100, \text{Round}(BaseLeverage \times M_{risk}))$$Section 12: API Race Conditions & SafetyContext: The bot performs a "Cancel-Then-Replace" operation when moving stops. This introduces a millisecond-level risk window.The Sequence:Cancel: api.cancelStopOrder(this.slOrderId).Gap: < 500ms where position is unprotected.Replace: api.placeStopOrder(...).Safety Mechanism (ReduceOnly):The bot strictly uses reduceOnly: true for all Stop and Limit exits.Why: If the user manually closes a position while the bot is trying to move a stop, a standard order might flip the position (open a short instead of closing a long).Result: reduceOnly guarantees the order is rejected if the position size is zero.Error Handling:If placeStopOrder fails (e.g., API overload), the bot catches the error in the try/catch block (line 554).Critical Design Note: In a future update, the bot should implement a "Retry Queue" for failed stop orders, as the position is currently left exposed if the replacement fails.
